Fix and refactor my existing Node.js (Express) web app navigation system and implement a premium bottom navigation bar with correct routing.

---

## GOAL

* Fix incorrect navigation behavior
* Create a premium bottom taskbar with 4 tabs:

  1. Dashboard
  2. Routine
  3. Hub
  4. Amy Coach (center highlight)

---

## CURRENT ISSUES TO FIX

* "Home" is opening Parent Hub → FIX (should open Dashboard)
* "Activities" is opening Routine → FIX mapping properly
* Missing Dashboard tab → ADD it back
* Incorrect route linking → FIX all routes

---

## FINAL ROUTE STRUCTURE

/dashboard → Dashboard (default home)
/routine → Routine page
/hub → Parent Hub
/amy-coach → AI Coach

---

## BOTTOM NAVBAR (PREMIUM UI)

HTML:

<div id="bottom-nav">

  <a href="/dashboard" class="nav-item" data-route="/dashboard">
    <span>🏠</span>
    <p>Dashboard</p>
  </a>

  <a href="/routine" class="nav-item" data-route="/routine">
    <span>📅</span>
    <p>Routine</p>
  </a>

  <a href="/amy-coach" class="nav-item center" data-route="/amy-coach">
    <div class="center-btn">🧠</div>
    <p>Amy Coach</p>
  </a>

  <a href="/hub" class="nav-item" data-route="/hub">
    <span>🧩</span>
    <p>Hub</p>
  </a>

</div>

---

## CSS (PREMIUM DESIGN)

#bottom-nav {
position: fixed;
bottom: 0;
width: 100%;
height: 75px;
background: rgba(15, 23, 42, 0.9);
backdrop-filter: blur(12px);
display: flex;
justify-content: space-around;
align-items: center;
border-top: 1px solid rgba(255,255,255,0.1);
z-index: 999;
}

.nav-item {
color: #aaa;
text-align: center;
font-size: 11px;
text-decoration: none;
position: relative;
}

.nav-item span {
font-size: 20px;
display: block;
}

.nav-item.active {
color: #6366f1;
}

.nav-item.active::after {
content: "";
position: absolute;
bottom: 6px;
left: 50%;
transform: translateX(-50%);
width: 6px;
height: 6px;
background: #6366f1;
border-radius: 50%;
}

.center {
transform: translateY(-20px);
}

.center-btn {
width: 60px;
height: 60px;
background: linear-gradient(135deg, #6366f1, #8b5cf6);
border-radius: 50%;
display: flex;
justify-content: center;
align-items: center;
font-size: 26px;
color: white;
box-shadow: 0 10px 25px rgba(99,102,241,0.5);
transition: transform 0.2s ease;
}

.center-btn:active {
transform: scale(0.9);
}

body {
padding-bottom: 90px;
}

---

## JS (ACTIVE TAB FIX)

const navItems = document.querySelectorAll(".nav-item");
const currentPath = window.location.pathname;

navItems.forEach(item => {
if (currentPath.includes(item.dataset.route)) {
item.classList.add("active");
}
});

---

## ROUTING FIX (VERY IMPORTANT)

* Ensure Express routes:

app.get("/dashboard", ...)
app.get("/routine", ...)
app.get("/hub", ...)
app.get("/amy-coach", ...)

* Set default route:
  app.get("/", (req,res) => res.redirect("/dashboard"))

---

## CLEANUP

* Remove old incorrect nav links
* Remove duplicate routes
* Ensure consistent naming across frontend + backend

---

## OUTPUT REQUIRED

1. Updated routing code
2. Fixed navigation bar
3. Correct tab behavior
4. Instructions to apply globally

---

## FINAL RESULT

* Dashboard opens correctly
* Routine opens correctly
* Hub opens correctly
* Amy Coach centered and highlighted
* Premium mobile app navigation feel

---
