import React from "react";
export const Ionicons = ({ name, size, color }: any) =>
  React.createElement("span", { "data-icon": name, style: { fontSize: size, color } });
export const MaterialCommunityIcons = ({ name, size, color }: any) =>
  React.createElement("span", { "data-icon": name, style: { fontSize: size, color } });
